<!DOCTYPE html>
<html>
<head>
  <title>Peluqueria</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Peluquería Unisex Katherinne</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <form class="navbar-form navbar-right">
            <div class="form-group">

            <% if current_user %>
            <div class="btn btn-primary" > <%= link_to 'Panel', "/panel" %> </div>
              <div class="btn btn-primary" > <%= link_to 'Cerrar Sesion', destroy_user_session_path, method: :delete %> </div>
            <% else %>
              <div class="btn btn-primary" > <%= link_to 'Registrarse', new_user_registration_path %> </div>
              <div class="btn btn-primary" > <%= link_to 'Iniciar Sesion', new_user_session_path %> </div>
              <% end %>


             
            </div>
            
              
            

              
              
              </p>
              
            </div>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

<!--<% if current_user %>
    <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
<% else %>
    <%= link_to 'Login', new_user_session_path %>
<% end %>-->

<%= yield %>

</body>
</html>


